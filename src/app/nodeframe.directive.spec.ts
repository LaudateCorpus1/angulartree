import { NodeframeDirective } from './nodeframe.directive';

describe('NodeframeDirective', () => {
  it('should create an instance', () => {
    const directive = new NodeframeDirective();
    expect(directive).toBeTruthy();
  });
});
